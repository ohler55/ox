;; -*- mode: outline; outline-regexp: " *[-\+]"; indent-tabs-mode: nil -*-

^c^d hide subtree
^c^s show subtree



- deal with loops (circular references)
 - optional parameter for dump
 - add id to attrs
 - maybe load but should be able grab id from the first object
 - keep cache table and put an object reference on each
 - add ref attribute to each object make a number or use the object_id

- UTF-8 support

rsync -vr --exclude ".*" --exclude "*~" --exclude "#*" --exclude misc ~/ox/ ~/git_ox

