;; -*- mode: outline; outline-regexp: " *[-\+]"; indent-tabs-mode: nil -*-

^c^d hide subtree
^c^s show subtree


- TBDs
 - add Bag class and auto create classes
  - classname2class(const char *name, int best_effort)
  - remove define_class from Bag
  - test
   - create xml by hand with undefined module and see if module and class are defined (extend Bad test)
   - verify modules are actually classes
    - need to allow for inner classes 

 - dump - free flag on error


#rsync -vru --exclude ".*" --exclude "*~" --exclude "#*" --exclude misc --exclude "*.o" ~/code/ox/ ~/git/ox
rsync -vrI --exclude ".*" --exclude "*~" --exclude "#*" --exclude misc --exclude "*.o" ~/code/ox/ ~/git/ox
rsync -vrI --exclude ".*" --exclude "*~" --exclude "#*" --exclude misc --exclude "*.o" ~/git/ox/ ~/code/ox

